<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorU</item>
        <item name="colorPrimaryDark">@color/colorU2</item>
        <item name="colorAccent">@color/colorU</item>
    </style>

    <style name="captioOnly">
        <item name="android:background">@null</item>
        <item name="android:clickable">false</item>
        <item name="android:focusable">false</item>
        <item name="android:minHeight">0dp</item>
        <item name="android:minWidth">0dp</item>
    </style>

    <style name="DialogoTitulo" parent="android:Theme.Holo.Dialog">

    </style>

</resources>
